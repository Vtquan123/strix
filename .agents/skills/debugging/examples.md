# Examples: Debugging

## Example 1 — Reproduce first
Write a failing test that reproduces the bug before touching code.

## Example 2 — Bisect
Narrow the fault by isolating inputs/commits until the smallest failing case
remains.
