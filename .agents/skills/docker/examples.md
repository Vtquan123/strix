# Examples: Docker

## Example 1 — Multi-stage build
Build in one stage, copy only artifacts into a slim runtime image to shrink size
and surface.

## Example 2 — Pin versions
Pin base image tags/digests for reproducible builds.
