# Examples: Git

## Example 1 — Branch + commit
Branch `feat/TASK-014-login-rate-limit`; commit
`feat(auth): add login rate limit (TASK-014)`.

## Example 2 — Small commits
Commit in logical steps so review + revert are easy.
