# Commands: Next.js

```bash
npm run dev            # next dev
npm run build          # next build
npm start              # next start (prod)
npm run lint           # next lint
```
_Match package.json scripts; escalate if a strategy change is implied._
