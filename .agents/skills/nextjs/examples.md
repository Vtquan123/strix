# Examples: Next.js

## Example 1 — Server component data fetch
Fetch on the server; pass typed props to a client component only where
interactivity is needed.

## Example 2 — Route handler
Add an API route following the project's request/response + error conventions.
