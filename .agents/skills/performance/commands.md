# Commands: Performance

```bash
# profile / benchmark with the project's tooling, e.g.:
node --prof src/...       # v8 profile
npm run bench             # project benchmark suite
# DB: EXPLAIN ANALYZE <query>
```
_Optimize only what you measured; escalate structural changes._
