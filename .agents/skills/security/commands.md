# Commands: Security

```bash
npm audit                 # dependency vulnerabilities
npm run lint              # security lint rules if configured
# project SAST/secret-scan hooks as configured
```
_A required convention change is a Claude decision — escalate, don't bend it._
