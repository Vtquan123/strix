# Commands: Testing

```bash
npm test                 # run suite
npm test -- --coverage   # coverage report
npm test -- path/to.test # focused run
```
_Match the project's runner (vitest/jest/playwright)._
