# Commands: TypeScript

```bash
npx tsc --noEmit       # type-check only
npm run build          # compile
npm run lint           # eslint (+ type-aware rules)
```
_Respect the project's tsconfig; escalate if strictness must change._
