# Router

The router documentation lives in one place:
[../workflow/router.md](../workflow/router.md) — the five Router functions, the
full routing table, capability-driven dispatch, the decision record, and the
invariants.

The routing data itself is [`config/routing.yaml`](../../config/routing.yaml).
