# Commands: Node

```bash
npm install            # deps
npm run build          # compile (tsc/bundler)
npm run lint
npm test               # unit + integration
node --watch src/...   # local run (adjust to project)
```
_Use the project's scripts; do not add new tooling without a task._
