# Examples: Node

## Example 1 — New endpoint
Mirror an existing route's validation, auth, and error shape; add integration
tests.

## Example 2 — Async error handling
Wrap async handlers so rejected promises return the standard error response, not
an unhandled rejection.
