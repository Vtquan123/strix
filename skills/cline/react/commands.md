# Commands: React

```bash
npm run dev            # local dev server
npm run build          # production build
npm run lint           # eslint
npm test -- --watch    # component tests (adjust to project runner)
```
_Use the project's actual scripts from package.json; do not invent new ones._
