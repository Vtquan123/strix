# Examples: React

## Example 1 — Controlled input
Mirror the existing form pattern; lift state per convention; add a test for
value + onChange.

## Example 2 — Effect cleanup
Subscriptions in `useEffect` return a cleanup fn to avoid leaks; covered by a
test that unmounts.
