# Examples: Testing

## Example 1 — Boundary cases
For a rate limiter: test under-limit, at-limit, over-limit, and reset.

## Example 2 — Regression test
Every bug fix adds a test that fails before the fix and passes after.
